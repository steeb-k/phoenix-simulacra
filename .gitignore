/target/
/dist/
.claude/
**/*.phnx
*.swp
.idea/
.vscode/
# Breadcrumb logs from the staged system-test runner.
t2-staged.log
arm64-t2.log
# Third-party payloads staged for the installer build, fetched and hash-verified
# by build-installer.ps1: the WinFsp MSI and the curated QEMU tree (~223 MB).
/installer/build/
# Azure Trusted Signing metadata (account + certificate-profile identifiers).
# Kept local so it isn't published to the public repo; provide it per build box.
/scripts/artifact-signing-metadata.json
